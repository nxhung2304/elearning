<% add_breadcrumb t('.title') %>

<section class="mx-auto max-w-6xl px-6 py-8">
  <div class="mb-6 flex items-center justify-between">
    <h1 class="text-2xl font-bold text-slate-900">
      <%= title_for(@course_categories) %>
    </h1>
    <%= link_to t("helpers.links.new", model: CourseCategory.model_name.human, default: "New CourseCategory"),
          new_course_category_path,
          class: "rounded-lg bg-emerald-600 px-4 py-2 text-sm font-medium text-white hover:bg-emerald-700" %>
  </div>

  <%= search_form_for @q, url: course_categories_path, html: { class: "mb-6" } do |f| %>
    <div class="flex gap-2">
      <%= f.search_field :name_cont,
            placeholder: CourseCategory.human_attribute_name(:name),
            class: "search-field" %>
      <%= f.submit t("helpers.search.submit", default: "Search"),
            class: "cursor-pointer rounded-lg bg-slate-100 px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-200" %>
    </div>
  <% end %>

  <div class="overflow-hidden rounded-xl border border-slate-200">
    <table class="w-full text-sm">
      <thead class="bg-slate-50">
        <tr>
          <th class="px-4 py-3 text-left font-medium text-slate-600"><%= CourseCategory.human_attribute_name(:name) %></th>
          <th class="px-4 py-3"></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-200">
        <% @course_categories.each do |course_category| %>
          <tr class="hover:bg-slate-50">
            <td class="px-4 py-3 text-slate-700"><%= course_category.name %></td>
            <td class="px-4 py-3 text-right">
              <%= link_to t("helpers.links.show", default: "Show"),
                    course_category_path(course_category),
                    class: "rounded-md border border-slate-300 px-4 py-1.5 text-xs font-medium text-slate-600 hover:border-slate-900 hover:text-slate-900 mr-2" %>
              <%= link_to t("helpers.links.edit", default: "Edit"),
                    edit_course_category_path(course_category),
                    id: "edit_course_category_#{course_category.id}",
                    class: "rounded-md border border-slate-300 px-4 py-1.5 text-xs font-medium text-slate-600 hover:border-slate-900 hover:text-slate-900" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="mt-4 flex justify-center">
    <%= pagy_nav(@pagy) if @pagy.pages > 1 %>
  </div>
</section>
